### P4: PHP Formulario
## EX_03

Crearemos en html donde pondremos un formulario con un textbox donde pondremos un numero que será operacion, en este caso

usaremos "$operacion = $_POST['operacion'];" guardaremos el resultado en la variable $operacion y con "$_POST['operacion']" nos devolvera el numero que hemos puesto en el textbox ahora con un blucle imprimiremos el text tantas veces como $operacion.length() 