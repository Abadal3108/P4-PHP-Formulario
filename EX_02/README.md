### P4: PHP Formulario
## EX_02

Crearemos en html donde pondremos un formulario con un textbox donde pondremos un string que será operacion, en este caso

usaremos "$operacion = $_POST['operacion'];" guardaremos el resultado en la variable $operacion y con "$_POST['operacion']" nos devolvera el string que hemos puesto en el textbox con el eval transformaremos en string a code ejecutable para que pueda hacer una operación.